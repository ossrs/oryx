#!/usr/bin/env bash

REALPATH=$(realpath $0)
WORK_DIR=$(cd $(dirname $REALPATH) && pwd)
echo "Run srs-terraform at $WORK_DIR from $0"

APP_ARGS=$@
echo "Program args: $APP_ARGS"

cd $WORK_DIR && node --title='srs-terraform listen 2022' index.js $APP_ARGS

