# SRS Plugin for aaPanel

To create a plugin for [aaPanel](https://www.aapanel.com).

## Usage

To create a `srs_cloud.zip` by:

```bash
bash auto/zip.sh
```

Upload the zip to aaPanel panel, and install SRS.

